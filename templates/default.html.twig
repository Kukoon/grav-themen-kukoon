{% extends 'partials/base.html.twig' %}

{% block content %}
	<h1 class="text-primary">{{ page.title }}</h1>
	{{ page.content|raw }}

	{% for module in page.collection() %}
		{{ module.content|raw }}
	{% endfor %}
{% endblock %}
